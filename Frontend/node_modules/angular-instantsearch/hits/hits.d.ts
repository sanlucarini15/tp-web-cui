import { TemplateRef } from '@angular/core';
import { BaseWidget } from '../base-widget';
import { Hit } from '../instantsearch/instantsearch';
export declare type HitsState = {
    hits: Hit[];
    results: {};
};
export declare class NgAisHits extends BaseWidget {
    instantSearchParent: any;
    template?: TemplateRef<any>;
    escapeHTML?: boolean;
    transformItems?: <U extends Hit>(items: Hit[]) => U[];
    state: HitsState;
    constructor(instantSearchParent: any);
    ngOnInit(): void;
    updateState: (state: any, isFirstRendering: boolean) => void;
}
